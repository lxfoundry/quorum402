# --- secrets: this repository is public and stays public. Never commit these ---
.env
.env.*
!.env.example
*.key
*.pem
secrets.json
**/mnemonic*
**/*seed-phrase*
# generated testnet accounts - contains private keys
.accounts.json
*.accounts.json

# --- dependencies ---
node_modules/
.pnp
.pnp.js
.yarn/

# --- build output ---
dist/
build/
out/
artifacts/
cache/
cache_hardhat/
typechain/
typechain-types/
coverage/
coverage.json
*.tsbuildinfo

# --- local-machine workarounds, never the repo's business ---
# A CA bundle carrying a TLS-inspecting proxy's root, and the compose override that mounts it.
subgraph/.certs/
subgraph/docker-compose.override.yml
!subgraph/docker-compose.override.example.yml
# graph-cli build output
subgraph/generated/
subgraph/build/

# --- tooling ---
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.vscode/
.idea/

# --- Claude Code local state (skills ARE committed, see CLAUDE.md) ---
.claude/settings.local.json
